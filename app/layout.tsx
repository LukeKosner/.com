import "./globals.css";

export const metadata = {
  title: "Luke Kosner",
  description: "Crafting Digital Experiences to Tell Stories.",
  icons: {
    icon: "/favicon.ico",
    apple: "/apple-touch-icon.png",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
